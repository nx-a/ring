server:
  port: ${SERVER_PORT:9080}
  static: ./static
service:
  name: ${SERVICE_NAME:ring}
  prod: ${PROD:false}
database:
  dsn: ${DATABASE_DSN:postgres://postgres:MRJ0i4vz9RvR@localhost:5432/ring?sslmode=disable&application_name=ring}